amends "elide:project.pkl"

name = "api-gateway"
description = "High-performance API gateway with routing, rate limiting, and load balancing"

entrypoint {
  "server.ts"
}

languages {
  typescript {
    enabled = true
    strict = true
  }
}

server {
  port = 8080
  host = "0.0.0.0"
}
